import java.io.*;
import java.util.*;

public class WordSearch
{
    public boolean exist(char[][] board, String word) 
    {
        Stack row = new Stack();
        Stack col = new Stack();
        int m = board.length, n=0;
        if(m>0)
        	n = board[0].length;

        return false;
    }
}